# Embodied Model Disagreement Protocols

Submission-hardening v2 artifact for paper 60 in the robotics 60-paper batch.

## Decision

Workshop-only.

The protocol is useful, but the evidence is a deterministic diagnostic. V2 hardening shows that executable physical probes are not always the rational choice once physical probe cost is counted.

## V2 hardening result

The utility stress uses `U = resolved - unsafe - cost_weight * probe_cost`. Executable probes win at cost weights 0.00 and 0.10. At cost weight 0.25, latent disagreement wins with utility 0.264 versus 0.207 for probes. At cost weight 1.25, abstention wins.

## Reproducible artifacts

- `docs/protocol_summary.json`: original protocol summary.
- `docs/probe_protocol_results.csv`: ambiguity-level results.
- `v2_probe_cost_sensitivity.py`: v2 utility stress.
- `docs/v2_probe_cost_sensitivity.csv`: v2 utility summary.
- `docs/v2_probe_cost_sensitivity.json`: v2 evidence payload.
- `paper/v2_probe_cost_table.tex`: manuscript table generated by the v2 script.
- `build_pdf.ps1`: rebuilds `C:/Users/wangz/Downloads/60.pdf` and removes `paper/main.pdf`.
